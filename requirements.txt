# RAG 하이브리드 검색 시스템 구축 따라하기 — 실습 의존성
# 설치: pip install -r requirements.txt
# 검증 환경: Python 3.10.18 / Windows 11 (macOS·Linux도 동일하게 동작)

# 문서 파싱
pdfplumber==0.11.9          # PDF 텍스트 추출 (한국어 읽기 순서가 정확)
pymupdf==1.26.5             # 비교용 (03-2에서 pdfplumber와 대조)

# 한국어 형태소 분석
kiwipiepy==0.20.1           # pip 단일 설치, Windows에서 별도 DLL 불필요

# SQLite 벡터 확장
sqlite-vec==0.1.6

# 임베딩 — OpenAI API와 자체 서버 모두 이 클라이언트 하나로 쓴다
openai==1.109.1

# 자체 임베딩 서버를 직접 띄울 때만 필요 (deploy/embed_openai_server.py)
# fastapi
# uvicorn
# FlagEmbedding

# PostgreSQL
psycopg[binary,pool]==3.2.3

# LLM 호출 (09~10장). 검색·검증만 돌릴 거라면 없어도 된다.
# 이 책은 모델 호출부를 실행하지 못했다 (09~10장의 [참고] 배지 참조).
anthropic==0.96.0

# 평가 (08-6). LLM 없는 검색 지표만 쓸 거라면 API 키가 없어도 된다.
ragas==0.3.0

# 대시보드 (10-3)
streamlit==1.27.1
pandas==2.3.2

# 검색 비교 데모 API (10-4). 화면은 src/static/index.html 한 파일이라 npm이 필요 없다.
fastapi==0.115.0
uvicorn==0.44.0

# 공통
numpy==1.26.4
requests==2.32.5
tiktoken==0.5.1             # 토큰 수 계산 (09장)
